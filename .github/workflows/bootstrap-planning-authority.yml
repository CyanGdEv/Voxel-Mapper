name: Bootstrap planning-authority architecture

on:
  workflow_dispatch:

# Temporarily disabled while the exact v0.12 source payload is staged.
jobs:
  staged:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Source payload staging in progress"
